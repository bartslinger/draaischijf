/*

After entering wifi config, make sure to run:
git update-index --assume-unchanged wificonfig.h


To start keeping track with git again, run:
git update-index --no-assume-unchanged wificonfig.h

*/

const char* ssid = "********";
const char* password = "********";
